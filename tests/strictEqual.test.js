const describe = require('mocha').describe
const it = require('mocha').it
const strictEqual = require('index.js')

describe('testing for strict equals', () => {
  it('if not strict equal then its false', () => {
    let result = strictEqual('5', 5)
    expect
  })
})

